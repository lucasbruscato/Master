library(pdftools)
fileNames <- list.files(path = ".")
fileNames
for (i in 1:(length(fileNames) - 2)) {
print(fileNames[i])
# fileNameNoExt <- substr(fileNames[i], 1, 4)
#
# text <- pdf_text(fileNames[i])
# textDivided <- strsplit(text, "\n")
#
# newNameNoExt <- paste0(fileNameNoExt,
#                        '-',
#                        trimws(gsub('\r', '', gsub(pattern = '/', '-', textDivided[[1]][7])), which = 'left'),
#                        '.pdf')
#
# file.copy(fileNames[i], newNameNoExt)
}
library(pdftools)
fileNames <- list.files(path = ".")
for (i in 1:(length(fileNames) - 2)) {
print(fileNames[i])
fileNameNoExt <- substr(fileNames[i], 1, 4)
text <- pdf_text(fileNames[i])
textDivided <- strsplit(text, "\n")
newNameNoExt <- paste0(fileNameNoExt,
'-',
trimws(gsub('\r', '', gsub(pattern = '/', '-', textDivided[[1]][7])), which = 'left'),
'.pdf')
file.copy(fileNames[i], newNameNoExt)
}
i = 1
print(fileNames[i])
fileNameNoExt <- substr(fileNames[i], 1, 4)
fileNameNoExt
fileNames[i]
length(fileNames[i])
nchar(fileNames[i])
nchar(fileNames[i])-4
substr(fileNames[i], 1, nchar(fileNames[i])-4)
library(pdftools)
fileNames <- list.files(path = ".")
for (i in 1:(length(fileNames) - 2)) {
print(fileNames[i])
fileNameNoExt <- substr(fileNames[i], 1, nchar(fileNames[i]) - 4)
text <- pdf_text(fileNames[i])
textDivided <- strsplit(text, "\n")
newNameNoExt <- paste0(fileNameNoExt,
'-',
trimws(gsub('\r', '', gsub(pattern = '/', '-', textDivided[[1]][7])), which = 'left'),
'.pdf')
file.copy(fileNames[i], newNameNoExt)
}
